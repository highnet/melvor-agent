{
  "name": "@melvor-agent/mod",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node --env-file-if-exists=../../.env build.mjs",
    "dev": "node --env-file-if-exists=../../.env build.mjs --watch",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@melvor-agent/shared": "workspace:*",
    "zod": "^3.24.1",
    "@melvor-agent/knowledge": "workspace:*"
  }
}
